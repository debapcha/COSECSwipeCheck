@*@using PagedList.Mvc
@model PagedList.IPagedList<COSECSwipeCheck.Models.Approver>*@
@model IEnumerable<COSECSwipeCheck.Models.Approver>

@{
    ViewData["Title"] = "Swipe";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*<style>
    .ul.pagination {
        display: inline-block;
        padding: 0;
        margin: 0;
    }

    ul.pagination li {
        display: inline;
    }

        ul.pagination li a {
            color: black;
            float: left;
            padding: 8px 16px;
            text-decoration: none;
            transition: background-color .3s;
        }

            ul.pagination li a.active {
                background-color: #4CAF50;
                color: white;
            }

            ul.pagination li a:hover:not(.active) {
                background-color: #ddd;
            }
</style>*@


<table class="table" border="1" style="position:absolute;margin-left:100px">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.USERID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EVENTDATETIME)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.USERID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EVENTDATETIME)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) 
                    @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
                </td>
            </tr>
        }
    </tbody>
</table>